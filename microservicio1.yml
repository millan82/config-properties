name: silvano
server:
  port: 1234

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/
  instance:
    instanceId: ${spring.application.name}:${random.value}    
